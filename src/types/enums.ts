export enum Pages {
  Home = '/',
  NewCensus = '/new-census',
  MyArchives = '/my-archives',
  Statistics = '/statistics',
  NotFound = '/not-found',
}

export enum FormScreens {
  Landing = 0,
  Contact = 1,
  Profile = 2,
  Lodging = 3,
  Summary = 4,
}
